CREATE TABLE [dbo].[ShoeSizeSelectors] (
    [Id]   INT            IDENTITY (1, 1) NOT NULL,
    [Name] NVARCHAR (MAX) NULL,
    CONSTRAINT [PK_ShoeSizeSelectors] PRIMARY KEY CLUSTERED ([Id] ASC)
);

